# Docker

Placeholder. The multi-stage `Dockerfile` and optional `compose.yaml` land in **M16**
(MVP release): one image that serves the built client static assets and the WebSocket
endpoint on a single port. See `docs/architecture.md` §15 and `docs/build-plan.md` M16.
